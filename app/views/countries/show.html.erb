<div class ="page-header">
  <%= link_to 'Edit', edit_country_path(@country), :class=> 'btn btn-warning pull-right' %>
  <%= link_to 'Back', countries_path, :class=> 'btn btn-default pull-right' %>
  <h3> <%= @country.name %></h3>
</div>

  <!-- Table -->
  <table class="table table-striped table-responsive">
    
  <thead>
    <tr>
      <th>Iso code 2</th>
      <th>Iso code 3</th>
    </tr>
  </thead>

  <tbody>
      <tr>
        <td><%= @country.iso_code_2 %></td>
        <td><%= @country.iso_code_3 %></td>
      </tr>
  </tbody>

  </table>



